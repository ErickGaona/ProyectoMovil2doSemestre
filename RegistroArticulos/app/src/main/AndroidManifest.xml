<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.high.developer.fingerprint_poc">

    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".principal.mostrarTodos"></activity>
        <activity android:name=".principal.eliminar" />
        <activity android:name=".principal.buscar6" />
        <activity android:name=".principal.editar" />
        <activity android:name=".PantallaTemporizada.cargando3" />
        <activity android:name=".PantallaTemporizada.inicio1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".huella.huella"
            android:noHistory="true" />
        <activity
            android:name=".MainActivity5"
            android:label="@string/title_activity_main" />
    </application>

</manifest>